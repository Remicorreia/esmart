{% extends 'base.html.twig' %}

{% block title %}eSmart / Smartphone{% endblock %}

{% block body %}

<div class="custom-container">
    <h1>Smartphone</h1>

    <table class="table">
        <tbody>
            <tr>
                        <td class="contimgsmartphones"><img class="imgsmartphones" src="{{ asset( 'images/' ~ smartphone.photo ) }}" alt="">
                        </td>
            </tr>
                <th>Nom</th>
                <td>{{ smartphone.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ smartphone.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ smartphone.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_smartphone_index') }}">back to list</a>

    <a href="{{ path('app_smartphone_edit', {'id': smartphone.id}) }}">edit</a>

    {{ include('smartphone/_delete_form.html.twig') }}
</div>
{% endblock %}
